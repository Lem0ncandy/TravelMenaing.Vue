import { getOneGuide } from '@/api/guide.js'

const actions = {
    // getOneGuide({ commit }, id) {
    //     return new Promise((resolve, reject) => {
    //         getOneGuide(id).then(response => {
    //             const { data } = response;
    //             console.log(data);
    //             resolve();
    //         }).catch(error => {
    //             reject(error)
    //         })
    //     })
    // }
}
export default {
    namespaced: true,
    actions
}